<%= form_with(
  model: current_user.target_users.find_by(
    target_user_id: @user.id
  ),
  html: { method: :delete }, local: false
) do |f| %>
  <%= f.submit "コラボ申請解除", class: "btn btn-default" %>
<% end %>
