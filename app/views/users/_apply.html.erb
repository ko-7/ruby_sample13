<%= form_with(model: current_user.target_users.build, local: false) do |f| %>
  <div><%= hidden_field_tag :target_user_id, @user.id %></div>
  <%= f.submit "コラボ申請する", class: "btn btn-primary" %>
<% end %>